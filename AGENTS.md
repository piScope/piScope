# Agent Instructions

## Signature Mapping
Before investigating, answering questions about, or modifying code, run
`sigmap ask "<question>"` (or `sigmap --query "<topic>"`) before file searches.

For manual regeneration, watcher, and setup guidance, see
`dev_notes/instruction_sigmap.md`.

## Test code generation rule
Before adding or editing plotting tests, read:

- `dev_notes/instruction_to_add_test.md`

Apply those conventions for file naming, quick/full mode intent, and shared test helpers.

## Coding Style
For coding style, read and follow `dev_notes/instruction_coding_style.md`.
